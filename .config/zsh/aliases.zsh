# Aliases
# Command shortcuts

# Dotfiles bare repo management
alias dotfiles='git --git-dir=$HOME/.dotfiles --work-tree=$HOME'

# Common shortcuts
alias ll='lsd -la'
alias la='lsd -A'
alias l='lsd -CF'

# Safety nets
alias rm='rm -i'
alias cp='cp -i'
alias mv='mv -i'
